---
# Ad-hoc packages kept via `bin/keep`. Managed by script — keep the
# two-space "  - name" list format. Promote entries to a dedicated
# role when they grow config beyond a bare install.
homebrew_packages:
  - ansible

homebrew_casks: []
